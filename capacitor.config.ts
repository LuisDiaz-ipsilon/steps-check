import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mx.uanl.cdis',
  appName: 'Steps Check',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
